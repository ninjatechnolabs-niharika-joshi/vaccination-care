# Environment Configuration Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
# ============================================================================
# IMPORTANT: For Supabase, you need BOTH URLs for optimal performance!
#
# 1. DATABASE_URL - Use the TRANSACTION POOLER URL (port 6543)
#    This provides connection pooling for fast queries (10-50ms instead of 1-2s)
#    Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
#
# 2. DIRECT_URL - Use the DIRECT CONNECTION URL (port 5432)
#    This is used for migrations and schema changes
#    Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
#
# Where to find these URLs:
# - Go to Supabase Dashboard > Project Settings > Database
# - Copy "Transaction pooler" URL for DATABASE_URL
# - Copy "Session pooler" or "Direct connection" URL for DIRECT_URL
# ============================================================================
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# JWT Configuration
# SECURITY: Generate a strong secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Must be at least 64 characters for production
JWT_SECRET=your-super-secret-jwt-key-min-64-chars-generate-with-crypto-randomBytes
JWT_EXPIRES_IN=7d

# CORS Configuration
# SECURITY: Set specific origins in production, NEVER use '*' in production!
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=*

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10

# Email Configuration
# Set ENABLE_EMAIL=false to skip email sending in development (API will still work)
ENABLE_EMAIL=false

# Email Configuration (SMTP)
# For Gmail: Use App Password (not regular password)
# Enable 2FA and create App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM_NAME=Vaxicare
EMAIL_FROM_ADDRESS=your-email@gmail.com

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3001

# OTP Configuration
OTP_EXPIRY_MINUTES=5
OTP_DEV_CODE=123456
OTP_RESEND_COOLDOWN_SECONDS=60
OTP_MAX_RESEND_ATTEMPTS=5
